{ config, pkgs, lib, ... }:
{
  # DEPRECATED: this file has been moved to `profiles/kde.nix`.
  #
  # Use `machines/example-machine.nix` as a starting point when creating a
  # real device deployment that composes machine defaults with this profile.
}
