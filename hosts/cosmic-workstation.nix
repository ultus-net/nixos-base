{ config, pkgs, lib, ... }:
{
  # DEPRECATED: this file has been moved to `profiles/cosmic.nix`.
  #
  # Profiles are now stored in `profiles/` and machine deployments live in
  # `machines/`. Use `machines/example-machine.nix` as a template to compose
  # a machine that imports `machines/configuration.nix` and a profile.
}
